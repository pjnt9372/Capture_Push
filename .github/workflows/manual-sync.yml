name: manual-sync

# 触发器：只允许通过 GitHub 界面手动点击运行
on:
  workflow_dispatch:
    inputs:
      run_reason:
        description: '执行原因/备注'
        required: false
        default: '执行手动同步任务'

jobs:
  manual-deploy:
    runs-on: ubuntu-latest
    name: 将 developer_space 推送到插件仓库

    steps:
      - name: 🛎️ 获取源代码
        uses: actions/checkout@v4

      - name: 🚀 推送至 Capture_Push_Plugin
        uses: peaceiris/actions-gh-pages@v3
        with:
          # --- 核心修改：使用 personal_token 而不是 github_token ---
          # 告诉 Action 使用你自己的 PAT 去操作，绕过 GITHUB_TOKEN 的限制
          personal_token: ${{ secrets.ACCESS_TOKEN }}
          
          # 目标仓库地址
          external_repository: https://github.com/pjnt9372/Capture_Push_Plugin.git
          
          # 要推送的目录
          publish_dir: ./developer_space
          
          # 目标分支
          publish_branch: main
          
          # 强制覆盖
          force_orphan: true
          
          # 提交信息
          commit_message: $
